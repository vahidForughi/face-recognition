# Face Recognition

To install this application, you need to have installed php +8.1

```
$ composer install
$ cp .env.example .env
$ php artisan key:generate
$ php artisan migrate
$ php artisan serve
```

```
$ npm install
$ npm run dev
```

